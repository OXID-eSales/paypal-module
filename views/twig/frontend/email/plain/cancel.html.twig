{% set shop = oEmailView.getShop() %}
{% set oViewConf = oEmailView.getViewConfig() %}
{% block paypal_email_plain_cancel_intro %}
{% if isPayPalOwnerMail %}
{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_INTRO_OWNER" }) }}
{% else %}
{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_SALUTATION" }) }} {{ order.oxorder__oxbillfname.getRawValue() }} {{ order.oxorder__oxbilllname.getRawValue() }},

{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_INTRO" }) }}
{% endif %}
{% endblock %}

{% block paypal_email_plain_cancel_details %}
{{ translate({ ident: "ORDER_NUMBER", suffix: "COLON" }) }} {{ order.oxorder__oxordernr.value }}
{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_ORDER_TOTAL", suffix: "COLON" }) }} {{ format_price(order.oxorder__oxtotalordersum.value, { currency: currency }) }}
{% if payPalRefundedAmount is not null %}
{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_REFUNDED", suffix: "COLON" }) }} {{ format_price(payPalRefundedAmount, { currency: currency }) }}
{% endif %}
{% endblock %}

{% block paypal_email_plain_cancel_note %}
{% if not isPayPalOwnerMail %}
{% if payPalRefundedAmount is not null %}
{{ translate({ ident: "OSC_PAYPAL_REFUND_MAIL_NOTE" }) }}
{% else %}
{{ translate({ ident: "OSC_PAYPAL_CANCEL_MAIL_NOTE_NO_REFUND" }) }}
{% endif %}
{% endif %}
{% endblock %}

{{ shop.oxshops__oxname.getRawValue() }}
{{ shop.oxshops__oxurl.value }}
